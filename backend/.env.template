# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=votre_email@example.com
SMTP_PASS=votre_mot_de_passe

# Optional: Override the "From" address
# If not set, defaults to SMTP_USER.
# Make sure your SMTP server authorizes this alias!
SMTP_FROM=noreply@rineau.eu
